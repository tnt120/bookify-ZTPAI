import { DivieWordsPipe } from './divie-words.pipe';

describe('DivieWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new DivieWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
