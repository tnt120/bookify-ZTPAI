import { FilterTranformLabelPipe } from './filter-tranform-label.pipe';

describe('FilterTranformLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTranformLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
